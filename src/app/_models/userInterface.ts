export interface UserInterface { 
    // class vs interface en .net. Una interfaz define un contrato, solo establece metodos a ser iimplementados en la clase 
    // class vs interface en Angular. En Angular son propiedades y metodos, donde se utilizan principalmente para propiedades  
    username: string; 
    token: string; 
}